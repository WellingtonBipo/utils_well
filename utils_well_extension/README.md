# Utils Well VSCode Extension

npm install

builder pra todos os arquivos => npx tsc
escutar => npx tsc -w

vsce package

code --install-extension utils-well-extension-0.0.1.vsix
Extensions → ⋮ → Install from VSIX
